name: TEST Scheduler

'on':
  workflow_dispatch:

  schedule:
    - cron: '*/5 * * * *'

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Print Time
        run: |
          echo "Scheduler Working"
          date
